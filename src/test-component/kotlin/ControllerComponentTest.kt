package com.rm.controller

import org.testng.Assert.assertEquals
import org.testng.annotations.Test

class ControllerComponentTest {
    @Test
    fun helloWorld()
    {
        assertEquals(true, true)
    }
}